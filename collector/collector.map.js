var collector = {}

//initialize constants for collector application
collector.createConstants = function(){
  
};

//create helpers and functions
collector.createFunctions = function(){
  
};

collector.initializeUI = function(){
  
};

collector.init = function(){
  collector.createConstants();
  collector.createFunctions();
  collector.initializeUI();
  
}